import {makeStyles} from '@rneui/themed'

export default makeStyles(theme => ({
  btnProgress: {
    position: 'absolute',
    top: '50%',
    left: '50%',
    marginTop: -12,
    marginLeft: -12
  },
  noWrap: {
    whiteSpace: 'nowrap'
  }
}))
